module greenlight.lennoxmugumira.net

go 1.25.7

//When there is a valid go.mod file in the root of your project directory, your project is a
//module.
//When you’re working inside your project directory and download a dependency with
//go get, then the exact version of the dependency will be recorded in the go.mod file.
//Because the exact version is known, this makes it much easier to ensure reproducible
//builds across different machines and environments.